# Uygulama kararları (Levo-Researches R01–R16'ya ek)

| # | Karar | Gerekçe |
|---|---|---|
| D01 | **R01 sapması:** Unity 6.3 yerine Python 3.14 + pygame-ce 2.5 + numpy, PyInstaller ile `.exe` | Makinede Unity/.NET SDK yok; hedef tek gecede EXE. İki katman (R02) korunur: `gh.engine/chart/timing/models` pygame import etmez, saf ve test edilebilir → ileride C# `Riff.Core`'a birebir taşınabilir. |
| D02 | *(D18 ile değişti.)* Oyun adı **RİFF** (R14) | "Guitar Hero" adı/ticari takdimi kullanılmaz; klasör adı `guitarhero` kullanıcı isteği olarak kaldı. |
| D03 | Mekanik değerleri Clone Hero/YARG varsayılanları (R05, R06): ±70 ms, 50/25, 1x→4x, SP %25/%50/8 ölçü, whammy 1/30 | Araştırmada üç implementasyonda doğrulanmış değerler. |
| D04 | Ses saati: pygame `Sound` stem'leri aynı anda başlatılır, saat `perf_counter` tabanlı monoton, `audio_offset` ile düzeltilir | pygame'de dspTime yok; tüm stem'ler RAM'de, aynı çağrıda başlar → sapma ihmal edilebilir. |
| D05 | Demo şarkılar tamamen sentezlenmiş, özgün (telif yok) | R14: lisanssız şarkı dağıtılmaz. Gitar stem'i chart'taki notaları çalar → "kaçırınca gitar kesilir" gerçekten çalışır. |
| D06 | `.mid` okuyucu kendi SMF parser'ı (bağımlılıksız) | R07 DryWetMidi C#'a özgü; Python'da mido gerekmez, format basit. |
| D07 | No Fail varsayılan açık (R13), ayarlardan kapatılabilir | — |
| D08 | ~~Klavye: A S J K L / ↑ ↓ / Space (açık nota) / H / ; / Enter~~ **DEPRECATED** → D09 | Clone Hero standardı (ek-01 §4.7). |
| D09 | Klavye (kullanıcı isteği, 2026-09-23): perdeler **1 2 3 4 5**, **Space = strum** (↑ ↓ de strum), H = SP, ; = whammy, Enter/Esc = duraklat. Ayrı açık-strum tuşu yok (açık notalar perdesiz strum ile). Ayarlar ana menü, şarkı listesi (Tab) ve duraklatma menüsünden açılır. `settings.json` v2: eski dosyaların tuşları yok sayılır. | Kullanıcı tercihi; Space açık-strum iken basılı perdeleri yok sayıp nota kaçırtıyordu. |
| D10 | *(Kısmen D12 ile değişti.)* **Otomatik chart (kullanıcı kararı, 2026-09-23):** hafif, yalnız numpy'lı kendi auto-charter'ımız (`gh/autochart`: çok bantlı spektral akı onset'leri, otokorelasyon + önsel ile tempo ve oktav seçimi, yerel tempo eğrili Ellis DP beat takibi, harmonik toplam perde → Viterbi ile perde konturu, kroma/tını öz-benzerliği ile bölümler) **Demucs / basic-pitch yerine** seçildi. Ses çözme pygame (SDL_mixer: MP3/OGG/WAV/FLAC/OPUS), etiketler kendi ID3/Vorbis/FLAC/WAV okuyucumuzla. | Tamamen çevrimdışı, exe içinde, yeni ağır bağımlılık yok (torch/librosa/scipy yok); 2 dk şarkı ~1 sn. **Sınırlar:** tek stem (song.*) → miss'te gitar kısılması uygulanmaz; notalar izole gitar partisini değil tüm miksin ritmini/melodisini izler (vokal, synth, davul vurguları da nota olabilir); 4/4 varsayılır (7/8 gibi bölümlerde ölçü çizgileri kayabilir); akor/perde seçimi sezgiseldir. Ölçüm: `tools/benchmark_autochart.py`. |
| D11 | Arayüz dili Türkçe / English (`gh/i18n.py`, `settings.json` `extra.language`; varsayılan Windows arayüz diline göre) | Kullanıcı isteği; oyun terimleri: Yıldız Gücü, Seri, Boşa vuruş, Kolay/Orta/Zor/Uzman; "Full Combo", "No Fail", "Whammy" olduğu gibi. |
| D12 | **Gitar kalibrasyonu (yapay zekâ):** D10'daki "Demucs / basic-pitch yok" kararı değişti. İçe aktarmada gitar **Demucs htdemucs_6s** ile ayrıştırılır, notaları **basic-pitch** ile çıkarılır; modeller ONNX (onnxruntime, CPU), STFT / son işleme numpy. Klasöre `guitar.ogg` + `song.ogg`. Gitar yoksa D10 yöntemine düşer. | Kullanıcı: miksten chart vokal / davulu da nota yapıyordu, gitarı izlemiyordu. torch yerine ONNX: exe içinde, çevrimdışı. 7,5 dk şarkı ~2 dk. |
| D13 | **Zayıf lead + senkron:** distorsiyonlu solo (ritim gitarıyla aynı kanalda) onset posteriorlarından ayrıca çıkarılır ve `E solo` ile işaretlenir; her nota gitar kanalındaki en yakın gerçek atağa oturtulur, ızgaraya yalnız ≤20 ms ise yapıştırılır. Kurallar hem gerçek şarkıda (Metallica "One") hem Demucs'la ayrılmış sentetik distorsiyonlu miksde doğrulanır. | "One"da solo sarı tuşta chug'dı; notalar "belirgin erken / geç"ti (≤20 ms %49 → %71). |
| D14 | **Elle yapılmış chart'lar Chorus Encore'dan** (`tools/chorus_fetch.py`, `.sng` okuyucu); yalnız Expert'i olanlara alt zorluklar Expert'ten seçilerek eklenir. Resmi oyun chart'ları varsayılan olarak seçilmez; kullanıcı isteğiyle (GH Live, GH serisi) indirildi. İndirilenler yalnız kullanıcının `Songs\` klasöründe, repoya / sürüme girmez. | Otomatik chart yaklaşık; birebir senkron için elle yapılmış chart gerekir (Guitar Hero'da da chart'lar elle yapılır). |
| D15 | **Arka plan videosu:** PyAV (FFmpeg) ile ayrı iş parçacığında çözme; şarkının kendi videosu ses konumunu izler, yoksa `assets/videos` altındaki 15 Pexels klibinden biri döngüde. Menülerde de. Ayardan kapatılabilir. | Kullanıcı isteği ("klibi varsa o, yoksa gitarist klipleri"). Pexels License yazılım içinde dağıtıma izin verir. |
| D16 | **Guitar Hero tarzı tema:** neon synthwave yerine gül ağacı sap, krom raylar, metal paneller, alev / kehribar / bronz palet (palet sabitlerinin adları geriye uyum için korundu). | Kullanıcı isteği. |
| D17 | **Kurulum konumu ve setlist'ler:** kullanıcının oyunu OneDrive dışına (`C:\Users\bicak\RIFF`) taşındı (~10 GB şarkı); `Songs\` altındaki üst klasörler setlist sayılır, şarkı listesi setlist'lere göre gruplanır. | OneDrive ücretsiz kotası 5 GB; 900 şarkılık listede gruplama gerekli. |
| D18 | **Oyunun adı \"Guitar Hero\" (kullanıcı kararı, 2026-09-24)**, logo `assets/logo.png` (kullanıcının verdiği görsel, `tools/make_logo.py` ile arka planı saydamlaştırıldı). Exe `GuitarHero.exe`, paket `GuitarHero-windows.zip`. İç adlar (`gh` paketi, \"RIFF Demo Band\", \"RIFF Auto\") değişmedi. | Kullanıcı isteği. **Risk:** \"Guitar Hero\" Activision'ın tescilli markası; depo / sürüm herkese açılırsa marka şikâyetiyle kaldırılabilir (D02'nin gerekçesi buydu). |
